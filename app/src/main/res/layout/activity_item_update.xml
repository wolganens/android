<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.wolgan.viagemfinal.items.ItemUpdateActivity">

    <Spinner
        android:id="@+id/categoryUpdateItem"
        android:layout_width="0dp"
        android:layout_height="46dp"
        android:layout_marginRight="16dp"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginLeft="16dp"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@+id/textUpdateItem" />

    <EditText
        android:id="@+id/textUpdateItem"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:ems="10"
        android:inputType="textPersonName"
        android:text="Name"
        android:layout_marginLeft="16dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="16dp"
        android:layout_marginRight="16dp"
        app:layout_constraintRight_toRightOf="parent" />

    <Button
        android:id="@+id/btnUpdateItem"
        android:layout_width="99dp"
        android:layout_height="wrap_content"
        android:onClick="updateItem"
        android:text="@string/btnUpdateItem"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@+id/categoryUpdateItem"
        android:layout_marginLeft="16dp"
        app:layout_constraintLeft_toLeftOf="parent" />

    <Button
        android:id="@+id/btnDeleteItem"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:onClick="deleteItem"
        android:text="@string/btnDeleteItem"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@+id/categoryUpdateItem"
        android:layout_marginRight="16dp"
        app:layout_constraintRight_toRightOf="parent" />
</android.support.constraint.ConstraintLayout>
